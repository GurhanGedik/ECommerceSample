@using ECommerce.Entity

@model IEnumerable<Brand>
@{
    ViewBag.Title = "List";
    Layout = "~/Areas/Admin/Views/Shared/_AdminPage.cshtml";
}

<header class="page-header">
    <h2>List Brand</h2>
    <div class="right-wrapper text-right">
        <ol class="breadcrumbs">
            <li>
                <a href="@Url.Action("AddBrand","Brand")" class="btn btn-primary text-white"><i class="fas fa-plus"></i> Add Brand</a>
            </li>
        </ol>

        <span class="sidebar-right-toggle"></span>
    </div>
</header>

@if (ViewBag.Resultt == true)
{
    <div class="alert alert-success">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <strong>Operation Successful</strong>
    </div>
}
else if (ViewBag.Resultt == false && ViewBag.Resultt != true)
{
    <div class="alert alert-danger">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <strong>Operation Failed</strong>
    </div>
}


<table class="table table-responsive-md mb-0">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(x => x.BrandName)</th>
            <th>@Html.DisplayNameFor(x => x.Description)</th>
            <th>@Html.DisplayNameFor(x => x.Photo)</th>
            <th>Edit & Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.BrandName</td>
                <td>@item.Description</td>
                <td><img src="~/Upload/@item.Photo" class="img-circle" style="width:75px" height="75" /></td>
                <td class="actions">
                    <a href="~/Admin/Brand/Edit/@item.BrandId"><i class="fas fa-pencil-alt"></i></a>
                    <a href="~/Admin/Brand/Delete/@item.BrandId" class="delete-row"><i class="far fa-trash-alt"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>





